from agagd.settings.dev import *

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'agagd.db',         # database name
        'USER': '',
        'PASSWORD': '',
        'HOST': '',         # blank means localhost
        'PORT': '',         # blank means default mysql port (3306)
    }
}
